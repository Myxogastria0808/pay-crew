{
  "name": "notify",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "catalog:notify",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:backend"
  }
}
